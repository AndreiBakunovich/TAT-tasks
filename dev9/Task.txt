Имеются две текстовые последовательности (строки). 
Заменить последовательную группу символов, выбранную случайно в одной строке, 
случайной группой последовательных символов, выбранной из другой. 
На консоль вывести исходные строки, и результат замены.
